# 电商客服 AI Agent 实习项目 · 面试通关文档

> 适用岗位：AI 应用开发 / AI Agent / 大模型应用开发（实习 / 校招）
> 使用方法：先读「第一～四部分」把项目讲清楚，再用「第五部分」对着面试官视角的问题逐条练习。每个问题都给了**考点 → 通俗解释 → 参考回答 → 可能追问**，按这个节奏背下来就能扛住拷打。
> 重要：本文是帮你把**你真实做过的事**讲清楚、讲专业，不是让你背一套假话。里面的数字（57%→83%、+13%、86 条用例等）请和你简历保持一致，能解释清楚「怎么来的」最关键，第六部分给了统一口径。

---

## 一、30 秒讲清楚这个项目（电梯陈述）

> 面试官第一句往往是「介绍一下你这段实习 / 这个项目」。先用下面这段把全貌讲清，再展开。

「我在广州智链信息科技实习，做的是一个**面向中小淘宝店铺的电商客服 AI Agent 原型**。简单说，它就是一个能替人工客服回答顾客问题的智能助手：顾客问『我的订单到哪了』『怎么退货』这类问题，系统先判断意图，能查数据的就**调用工具**（查订单、查物流），属于政策/规则类的就走 **RAG 知识库**检索后回答，遇到搞不定的再转人工。

我在里面主要负责三块：**①** 用 Python + LangChain 开发了订单查询、物流跟踪、退换货指引这 3 个工具调用功能；**②** 做 RAG 知识库的数据治理，把店铺历史客服对话清洗成可用的知识；**③** 做提示词策略对比，并写测试用例、整理用户反馈。因为是第一份实习，核心算法和架构是带我的工程师定的，我主要负责把这几个模块**落地、调通、测稳**。」

**一句话总结（可作为收尾）：** 这段实习让我完整走了一遍「需求 → 工具开发 → 数据治理 → 提示词调优 → 测试 → 收集反馈」的 AI 应用落地流程。

---

## 二、项目架构：你必须能画出来 + 讲出来

### 2.1 整体数据流（画在纸上长这样）

```
顾客提问
   │
   ▼
┌──────────────┐   ① 意图识别 / 路由（Router）
│  意图分类     │   判断这句话属于：订单类 / 政策咨询类 / 投诉类 / 闲聊
└──────┬───────┘
       │ 分流
   ┌───┴─────────────┬──────────────────┐
   ▼                 ▼                  ▼
┌────────┐      ┌───────────┐       ┌──────────┐
│工具 Agent│      │知识 Agent  │       │ 转人工兜底│
│Function │      │（RAG 检索）│       │（解决不了）│
│ Calling │      │           │       │           │
└────┬────┘      └─────┬─────┘       └────┬─────┘
     │ 查订单/物流/退换货  │ 检索知识库片段        │ 打包上下文给人工
     └─────────┬─────────┴───────────────────┘
               ▼
        ┌──────────────┐  ② 汇总（Summary）
        │ LLM 生成回复   │  把工具结果 / 知识片段组织成一句自然、礼貌的话
        └──────┬───────┘
               ▼
         流式返回给顾客（逐字输出）
```

**这张图你要能边画边讲。** 关键点：**先分流、再分别处理、最后统一组织语言**。这是目前电商客服 Agent 的主流做法（业界叫 multi-agent / router 架构）。

### 2.2 三条处理路径分别是什么

| 路径 | 处理什么问题 | 怎么实现 | 你负责的部分 |
|---|---|---|---|
| **工具 Agent** | 需要查实时数据的：订单状态、物流到哪了、能不能退换货 | **Function Calling（工具调用）**，每个能力是一个函数 | ✅ 你开发的 3 个工具 |
| **知识 Agent** | 政策/规则类：发货时效、退换货政策、优惠规则、商品 FAQ | **RAG**：从知识库里检索相关片段，再让 LLM 据此回答 | ✅ 你做的知识库数据治理 |
| **转人工兜底** | 系统答不了、用户要投诉、情绪激动 | 打包对话摘要转交人工客服 | 了解即可（一般不是实习生主导）|

### 2.3 技术栈（被问到「用了什么技术」时回答）

- **语言/框架**：Python + **LangChain**（用它把 LLM、工具、提示词串起来）
- **大模型**：通过 API 调用（如 OpenAI / 国产大模型，按公司而定）
- **RAG 相关**：文本向量化（Embedding）+ 向量库做相似度检索
- **工具调用**：LangChain 的 `@tool` + 工具调用 Agent（`create_tool_calling_agent` + `AgentExecutor`）
- **服务**：后端接口（如 FastAPI），回复用**流式输出（SSE）**逐字返回，体验更好

> 提示：如果你实际用的模型、向量库名字记得，就讲具体名字；记不清就说「通过大模型 API 调用」「用了向量检索」，不要硬编一个你没用过的产品名，避免被追问细节露馅。

---

## 三、术语速查表（先看懂这些，回答才不虚）

> 面试官经常用术语「设套」，你先把这些用大白话理解透。

| 术语 | 一句话大白话 | 在本项目里指什么 |
|---|---|---|
| **LLM / 大模型** | 会「读懂文字、生成文字」的 AI，比如 GPT | 客服回答的「大脑」 |
| **AI Agent（智能体）** | 不只是会聊天，还能**自己决定调用工具去干活**的 AI | 我们的客服系统整体 |
| **Function Calling / 工具调用** | 让大模型在需要时「点名」调用某个函数（如查订单），拿到结果再回答 | 我开发的订单/物流/退换货 3 个工具 |
| **Tool（工具）** | 一个被包装好、能给 Agent 调用的函数 | `query_order()` 等 |
| **Prompt（提示词）** | 你给大模型下的「指令 + 背景」，决定它怎么回答 | 客服话术模板、角色设定 |
| **Zero-shot（零样本）** | 不给例子，直接让模型答 | 提示词策略之一 |
| **Few-shot（少样本）** | 在提示词里给几个「问—答」示例，模型照着学 | 我们最终采用的方案核心 |
| **CoT（思维链）** | 让模型「一步步想」再答，适合需要推理的题 | 对比实验里的一种策略 |
| **RAG（检索增强生成）** | 先去知识库「查资料」，再让大模型「照着资料回答」，减少瞎编 | 政策/FAQ 类问题的处理方式 |
| **Embedding（向量化）** | 把一句话变成一串数字（向量），意思相近的句子数字也相近 | 知识库检索的基础 |
| **向量检索 / 相似度检索** | 用向量比「谁和问题最像」，找出最相关的知识片段 | RAG 的检索环节 |
| **Chunk / 分块** | 把长文档切成一小段一小段再存进知识库 | 数据治理里的「分段标准化」 |
| **Rerank（重排序）** | 检索出一批候选后，再精排一次，把最相关的排前面 | 进阶检索优化（了解即可）|
| **幻觉（Hallucination）** | 大模型「一本正经地胡说八道」 | RAG 就是为了压住它 |
| **召回率 / 匹配率** | 该找到的相关内容，实际找到了多少比例 | 我说的 57%→83% 就是这个意思 |
| **多轮对话 / 上下文** | 记住前面聊过的内容，不用用户每次重复 | 客服必备能力 |
| **流式输出（SSE）** | 回答像打字机一样一个字一个字蹦出来，而不是憋半天一次性出 | 提升体验 |

---

## 四、把简历每条经历「讲成 2 分钟的故事」

> 面试官会盯着简历每一条问。下面把你简历的 5 条 bullet 各自展开成「背景—做法—细节—结果—收获」，练熟后任意一条都能讲 1～2 分钟。

### 4.1 工具调用模块（订单查询 / 物流跟踪 / 退换货指引）

- **背景**：客服 60% 以上的问题其实是高度重复的「我的订单状态」「快递到哪了」「怎么退货」，这类问题答案在系统/数据库里是确定的，不需要大模型「创作」，最适合用工具调用解决。
- **做法**：我用 LangChain 的 `@tool` 把每个能力封装成一个函数，写清楚函数的**功能说明（docstring）和入参**——因为大模型就是靠这段说明判断「该不该用、用哪个、传什么参数」。
  - `query_order(order_id)`：按订单号查订单状态、商品、预计送达
  - `track_logistics(order_id)`：查物流轨迹/最新节点
  - `return_guide(order_id, reason)`：判断是否可退换并给出指引/生成工单
- **细节（能体现你真的写过）**：
  - 入参校验：订单号格式不对/查不到，要返回**友好提示**而不是报错堆栈。
  - 异常兜底：外部接口超时/失败时，工具返回「暂时查不到，请稍后或转人工」，不能让整个对话崩。
  - 让 Agent 选对工具：靠**清晰的工具描述**区分「查订单」和「查物流」，描述写含糊它就会调错。
- **结果**：3 个工具开发+联调上线，覆盖约 60% 的标准化咨询。
- **收获**：理解了 Agent「**思考→选工具→执行→看结果→回答**」的闭环，以及工具描述写得好不好直接决定 Agent 调用准不准。

### 4.2 RAG 知识库数据治理（57% → 83%）

- **背景**：政策/FAQ 类问题（退换货政策、发货时效）要靠知识库回答。原始素材是 3 家店铺约 **1.8 万条**历史客服对话，又脏又乱：大量重复、闲聊、答非所问、一段里混多个话题，直接喂进去检索效果很差。
- **做法（三级清洗）**：
  1. **去重**：合并/删掉重复和高度相似的对话（避免知识库里一堆几乎一样的东西，稀释检索质量）。
  2. **无效过滤**：去掉纯闲聊、表情、「在吗」「好的」这类没有信息量的对话，以及答非所问的脏数据。
  3. **分段标准化**：把有效问答整理成「**问题—标准答案**」结构，长文本按语义切成合适大小的 **chunk**，统一格式后再入库。
- **结果**：知识库「有效问答匹配率」从 **57% 提升到 83%**。
- **收获**：亲身体会到「**RAG 效果的下限是数据决定的**」——不是模型不行，是喂进去的资料太脏。

### 4.3 提示词策略对比实验（+13% 准确率）

- **背景**：同样的知识、同样的模型，**提示词写法不同，回答质量差很多**。需要找到最适合客服场景的写法。
- **做法**：固定模型和测试集，对比三种主流策略：
  - **零样本**：直接问 → 简单但回答风格不稳定、不够「客服味」。
  - **少样本**：给几个标准「问—答」示例 → 回答格式、语气更可控。
  - **CoT 思维链**：让模型一步步推理 → 对需要判断的场景（如「能不能退」）有帮助，但**更费 token、更慢**，简单问答上性价比低。
  - 最终方案：「**少样本 + 场景化引导**」——给示例统一话术风格，再针对不同场景（售前/售后/物流）加针对性引导。
- **结果**：问答准确率提升约 **13%**，平均响应时间缩短约 **0.5 秒**（因为没盲目全用 CoT，省了推理开销）。
- **收获**：提示词不是玄学，要靠**对比实验 + 固定测试集**来选，而不是凭感觉。

### 4.4 测试用例与缺陷修复（86 条用例 / 16 个 bug）

- **背景**：客服直接面对用户，答错代价高，上线前必须系统测试。
- **做法**：设计 **86 条**测试用例，重点覆盖**边界场景**：
  - 常规提问（正常订单号、标准问法）
  - **模糊表述**（「我那个东西啥时候到」——没给订单号）
  - **异常输入**（瞎编的订单号、空输入、夹杂表情/错别字）
- **结果**：定位并协助修复 **16 个** bug（多为工具调用传参错误、话术不当、边界没兜住），把版本自测周期从 2 天压到 1 天。
- **收获**：学会从「用户会怎么乱问」的角度去想测试，而不只测「正常路径」。

### 4.5 用户反馈整理（12 位店主 / 3 项需求落地）

- **做法**：收集 12 位试用店主反馈，按「**功能缺陷 / 体验优化 / 新增需求**」分类，输出分析文档给产品。
- **结果**：其中 3 项核心需求被纳入内测版本。
- **收获**：理解了技术不是闭门造车，要看真实用户怎么用、卡在哪。

---

## 五、资深面试官拷打 Q&A（核心）

> 每题结构：**【考点】**面试官想验证什么 → **【通俗解释】**相关概念 → **【参考回答】**可直接背 → **【可能追问】**及应对。

### A. 项目与角色（开场必问）

**Q1：介绍一下你这个项目，以及你具体负责什么？**
- **【考点】**能不能讲清全貌、边界清不清楚、有没有「贪功」。
- **【参考回答】**用第一部分的电梯陈述，最后**主动划清边界**：「架构和核心方案是带我的工程师定的，我负责工具模块开发、知识库数据治理和提示词调优、测试这几块的落地。」
- **【可能追问】**「哪部分是你独立做的？」→ 答：3 个工具的开发联调、知识库清洗流程、测试用例是我独立完成的；架构设计是参与讨论、跟着做。

**Q2：为什么用 AI Agent，而不是传统的关键词匹配客服机器人？**
- **【通俗解释】**传统机器人靠「关键词→固定回答」，用户换个说法就答不上；Agent 能理解意图、能调工具查实时数据、能多轮对话。
- **【参考回答】**「传统规则客服覆盖面窄、维护成本高，用户稍微换个问法就匹配不上。用 Agent 一是能理解自然语言意图，二是能**调用工具查实时数据**（订单/物流），三是能结合知识库回答政策问题，覆盖的问题类型多很多。」
- **【可能追问】**「那为什么不直接用大模型聊天就行？」→ 因为大模型**不知道实时订单数据**（要靠工具），也**容易编造政策**（要靠 RAG 约束），所以要 Agent + 工具 + RAG 组合。

**Q3：这个项目最大的难点是什么？你印象最深的问题？**
- **【参考回答】**挑一个真实的小故事，例如：「最头疼的是 Agent 有时该查物流却调成了查订单。后来发现是两个工具的**描述写得太像**，模型分不清。我把工具描述改得更明确（明确各自的适用问题和触发词），调用准确率明显提升。」——这种「小而真实」的难点最可信。

---

### B. 工具调用 / Function Calling

**Q4：Function Calling 的原理是什么？大模型怎么知道要调用工具？**
- **【通俗解释】**你把每个工具的「名字、功能说明、需要什么参数」告诉大模型；用户提问后，模型判断「这个问题需要用某个工具」，就输出一个**结构化的调用请求**（工具名 + 参数）；程序真正执行函数，把结果再喂回给模型，模型据此组织最终回答。
- **【参考回答】**「我用 LangChain 的 `@tool` 装饰器把函数注册成工具，函数的文档字符串会作为工具描述给到模型。模型看到用户问『订单到哪了』，就会判断该调 `track_logistics` 并解析出订单号作为参数；程序执行后把物流结果返回，模型再用自然语言回复用户。整个『思考→调用→拿结果→回答』由 AgentExecutor 驱动。」
- **【可能追问】**
  - 「工具描述写不好会怎样？」→ 模型会**调错工具或不调**，所以描述要清晰、互相区分。
  - 「参数解析错了怎么办？」→ 工具内做**入参校验**，不合法就返回友好提示，引导用户补充信息。

**Q5：如果一个问题需要连续调用多个工具（比如先查订单再判断能不能退），怎么处理？**
- **【通俗解释】**这就是 Agent 的「多步推理」：调完一个工具拿到结果，再决定下一步调哪个。
- **【参考回答】**「Agent 是循环式的：调 `query_order` 拿到状态（如『已签收』），把结果作为新的已知信息，再决定是否调 `return_guide`。LangChain 的 AgentExecutor 会自动跑这个**多轮工具调用循环**，直到模型认为信息够了再生成最终回答。」
- **【可能追问】**「会不会无限循环？」→ 会设**最大步数**上限，超过就停止并转人工/给兜底回复。

**Q6：工具调用失败（接口超时、查不到）你怎么兜底？**
- **【参考回答】**「工具内部 try/except，失败返回结构化的友好信息（如『暂时查询不到，建议稍后重试或转人工』），而不是抛异常让对话崩。同时把这种情况计入测试用例，保证异常路径也被覆盖。」

---

### C. RAG 知识库（最容易被深挖）

**Q7：什么是 RAG？为什么不直接问大模型，要先检索？**
- **【通俗解释】**RAG = 先「查资料」再「照着答」。不查资料直接问，大模型可能**编造**一个听起来对、其实错的政策。
- **【参考回答】**「大模型不知道这家店的具体退换货政策，硬答容易产生**幻觉**（一本正经胡说）。RAG 的做法是：先把用户问题去知识库里检索出最相关的政策/FAQ 片段，再把『问题 + 检索到的片段』一起给模型，让它**基于真实资料回答**，这样答案可控、可追溯。」
- **【可能追问】**「检索是怎么做的？」→ 把知识切成 chunk 并向量化存进向量库，用户问题也向量化，算**相似度**找最接近的几段。

**Q8（重点）：你说匹配率从 57% 提到 83%，这两个数字是怎么测出来的？**
- **【考点】**数字真不真、有没有方法论。这是最常见的「拆台」问法，一定要有统一口径（见第六部分）。
- **【参考回答】**「我们抽了一批**真实用户问题做测试集**（人工标注每个问题对应的正确答案在不在知识库里）。让系统检索，看返回的 Top 片段里**有没有命中正确答案**，命中算成功。清洗前命中率约 57%，清洗后约 83%。提升主要来自去掉脏数据和重复、把问答结构化，让检索更容易命中。」
- **【可能追问】**
  - 「测试集多大？」→ 给一个合理量级（如几百条抽样），强调是**真实问题**而非编的。
  - 「为什么清洗就能提这么多？」→ 脏数据/重复会**稀释和干扰**检索，正确片段被淹没；清洗后正确片段更突出，自然更容易被检索到。

**Q9：「三级清洗」具体是哪三级？去重你怎么去的？**
- **【参考回答】**「①去重：把内容重复或高度相似的对话合并，避免知识库里一堆几乎一样的条目。②无效过滤：去掉纯闲聊、无信息量（『在吗』『好的』）和答非所问的脏数据。③分段标准化：把有效内容整理成『问题—标准答案』结构，长文本按语义切成合适大小的 chunk，统一格式入库。」
- **【可能追问】**「相似的怎么判断？」→ 可以从简单到进阶讲：先按规则（完全重复、模板话术）去，再用**文本相似度**判断高度相似项。**别硬说复杂算法**，能讲清思路即可：「我主要做的是规则+相似度筛选，更复杂的算法是团队同学负责的。」（诚实划界）

**Q10：分块（chunk）切太大或太小会怎样？**
- **【通俗解释】**切太大：一段里混了好几个话题，检索到也不精准；切太小：一句话被切断，丢了上下文。
- **【参考回答】**「chunk 太大容易一段混多个主题，检索『看着相关其实答非所问』；太小会丢上下文。所以按**语义/问答边界**切，让每个 chunk 尽量是一个完整的小知识点。」

**Q11：检索不准、答错了怎么排查？**
- **【参考回答】**「先看是**检索问题**还是**生成问题**：把检索到的片段打出来看——如果片段就不对，是数据/分块/检索的问题（去优化知识库或检索）；如果片段对、回答错，是**提示词**或模型的问题（去调提示词）。」——这套「分层定位」思路面试官很爱听。

---

### D. 提示词工程

**Q12：零样本、少样本、CoT 有什么区别？你为什么选少样本？**
- **【通俗解释】**见第三部分术语表。
- **【参考回答】**「零样本最省事但风格不稳；CoT 让模型一步步推理，适合需要判断的复杂题，但**更慢更费 token**，简单客服问答上不划算；少样本给几个标准问答示例，能很好地统一**话术风格和格式**，最适合客服这种『要求语气专业一致』的场景。所以我们以少样本为主，再针对不同场景加引导。」
- **【可能追问】**「那需要推理的场景（比如判断能不能退）怎么办？」→ 在这类场景**局部用 CoT 或在提示里给判断步骤**，不是全程都用，平衡效果和速度。

**Q13：+13% 的准确率怎么测的？怎么保证是提示词的功劳而不是别的变量？**
- **【参考回答】**「做**对照实验**：**固定模型、固定知识库、固定测试集**，只改提示词策略，分别跑同一批测试问题，由人工或规则判定回答对错，比较准确率。控制变量后，差异就能归因到提示词。」

**Q14：提示词太长 / token 太多怎么办？**
- **【参考回答】**「示例不是越多越好，挑**最有代表性的几个**；知识片段只放检索到的 Top 几条，不是全塞；能用简洁指令说清的就不啰嗦。这样既控制成本又保证效果。」

---

### E. 测试与质量

**Q15：你的 86 条用例是怎么设计的？举一个边界用例。**
- **【参考回答】**「我按场景分类设计：正常路径、模糊表述、异常输入。比如一个边界用例是用户说『我那个东西怎么还没到』——**没给订单号**。系统应该**反问让用户补订单号**，而不是瞎查或报错。还有瞎编的订单号要返回『查不到』的友好提示。」

**Q16：举一个你修过的 bug。**
- **【参考回答】**挑真实的讲，例如：「用户输入带表情或多余空格时，订单号解析失败导致工具报错。修复是在工具入参里做**清洗和格式校验**，提取出合法订单号再查，查不到就友好提示。」

---

### F. 软性 / 陷阱题（最容易翻车，重点练）

**Q17：这是你第一份实习，哪些是你真正动手做的，哪些是团队做的？**
- **【考点】**诚实度。**别贪功**，划清边界反而加分。
- **【参考回答】**「工具模块开发联调、知识库清洗流程、测试用例和反馈整理是我独立完成的；整体架构、模型选型、向量库这些底层方案是带我的工程师定的，我是参与和执行。作为实习生，我的定位就是把分到的模块踏实做好、测稳。」

**Q18：数据从哪来的？有没有用户隐私问题？**
- **【参考回答】**「数据是店铺自己的历史客服对话，**在公司内部、授权范围内**使用。清洗时我们会**去掉/脱敏**手机号、地址等个人敏感信息，只保留问答语义。」——主动提隐私意识是加分项。

**Q19：如果让你重做这个项目，你会怎么改进？**
- **【参考回答】**「一是检索环节加上**重排序（rerank）**进一步提升准确率；二是把测试做成**自动化回归**，每次改动自动跑一遍用例；三是补充更细的**线上效果监控**（哪些问题答得差），形成持续优化闭环。」

**Q20：你觉得这套系统现在还有什么不足 / 它答不好的情况？**
- **【参考回答】**「对**没进知识库的新政策**、或**极度口语化/多意图混在一句**的问题还容易答偏，这类目前靠转人工兜底。要解决得靠持续补充知识库和更强的意图拆分。」——能说出局限，说明你真的理解它。

**Q21：你怎么衡量这个客服 Agent「好不好」？**
- **【参考回答】**「主要看几个指标：**回答准确率**（答得对不对）、**意图识别/工具调用准确率**、**知识库命中率**、**响应时间**，以及业务侧的**转人工率**（越低说明自动解决得越多）和用户满意度。」

**Q22：LangChain 在这里起什么作用？不用它行不行？**
- **【参考回答】**「LangChain 帮我们把『大模型 + 工具 + 提示词 + 多轮调用循环』这些组件**标准化地串起来**，少写很多胶水代码。不用它也能用原生 API 自己实现工具调用循环，只是要自己处理调用解析、循环控制这些，开发更慢。它本质是个**工程脚手架**。」

---

## 六、数字统一口径（被追问时不能前后矛盾）

> 这些数字一旦写进简历，面试官就可能逐个问「怎么算的」。请记住统一说法，**前后一致**比数字大小更重要。

| 数字 | 统一口径（被问就这么答） |
|---|---|
| **覆盖约 60% 标准化咨询** | 指订单/物流/退换货这类**高频、答案确定**的问题，约占客服总咨询量的六成，这部分能被 3 个工具自动处理 |
| **1.8 万条对话** | 3 家淘宝店铺的历史客服聊天记录总量（清洗前的原始量级）|
| **57% → 83%（匹配率）** | 抽一批真实用户问题做测试集，人工标注正确答案；看检索 Top 片段是否命中正确答案。清洗前命中约 57%，清洗后约 83% |
| **+13% 准确率** | 固定模型/知识库/测试集，只改提示词策略做对照实验，人工或规则判对错后对比 |
| **响应时间 -0.5 秒** | 没有盲目全程用 CoT（CoT 推理更慢），简单问答用少样本，平均省下约 0.5 秒 |
| **86 条用例 / 16 个 bug** | 86 条覆盖正常+模糊+异常三类场景；16 个 bug 多为传参错误、话术不当、边界未兜底 |
| **12 位店主 / 3 项需求** | 试用期收集 12 位店主反馈，分三类整理，3 项被采纳进内测版本 |

> **应对「数字怎么这么准」的话术：** 「这是当时实习记录里的大致数据，主要想说明清洗/调优**带来了明显提升**，具体数值是当时测试集上的结果。」——稳、不心虚。

---

## 七、一分钟自我介绍模板（开场用）

「面试官好，我是庞业祺，软件工程专业。我在广州智链信息科技做过一段 **AI 应用开发**实习，方向是**电商客服 AI Agent**。我主要用 Python + LangChain 开发了订单查询、物流、退换货 3 个**工具调用**功能，做了 **RAG 知识库的数据治理**（把匹配率从 57% 提到 83%），还做了**提示词策略对比**和测试。这段实习让我完整跑通了一个大模型应用从开发、数据治理到调优测试的落地流程。平时我也用 Codex、Claude Code 这类 AI 工具辅助开发。我对 AI Agent 方向很有兴趣，也是我想长期投入的方向。」

---

## 八、考前自检清单（能全部「✓」就稳了）

- [ ] 能在 30 秒内讲清项目是干嘛的、我负责啥
- [ ] 能徒手画出「意图识别 → 工具/RAG/转人工 → 汇总回复」的架构图
- [ ] 能说清 Function Calling 原理（思考→选工具→执行→回答）
- [ ] 能说清 RAG 为什么需要、检索怎么做、为什么能压幻觉
- [ ] 能讲清三级清洗、以及 57%→83% 怎么测的
- [ ] 能讲清零样本/少样本/CoT 区别，以及为什么选少样本
- [ ] 能说清 +13% 是怎么做对照实验测出来的
- [ ] 能举 1 个真实 bug、1 个边界测试用例
- [ ] 被问「哪些是你独立做的」能诚实划清边界、不贪功
- [ ] 第六部分的数字口径全部记熟、前后一致

> 记住：实习生不需要把自己讲成架构师。**把你真正做的几块讲扎实、讲明白、数字解释得清楚**，比吹得高深更能打动面试官。
